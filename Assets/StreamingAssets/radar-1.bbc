using "lib/api";
using "lib/protocol";

void Receive()
{
    byte[MaxPacketLength] v;
    float3 direction;

    while (1)
    {
        int receivedLength = wreceive(&v, MaxPacketLength, &direction);
        if (!receivedLength) { break; }

        if (v[0] == (byte)PacketHeader.request &&
            receivedLength == sizeof(VerificationRequestPacket))
        {
            VerificationRequestPacket* packet = &v as VerificationRequestPacket*;

            if (Logs)
            {
                print("I-REQ ");
                print(packet.position);
                printline();
            }

            if (distancesqr(packet.position, GPS.position) < DistanceThresholdSq)
            {
                if (Logs) print(" M");
                VerificationResponsePacket responsePacket = new VerificationResponsePacket;
                responsePacket.header = PacketHeader.response;
                responsePacket.position = GPS.position;
                if (Debug) debug(packet.position, Color.Cyan);
                wsend(&responsePacket as byte[]*, sizeof(VerificationResponsePacket), &direction);
            }
        }
    }

    while (1)
    {
        int receivedLength = receive(&v, MaxPacketLength);
        if (!receivedLength) { break; }

        if (v[0] == (byte)PacketHeader.request &&
            receivedLength == sizeof(VerificationRequestPacket))
        {
            VerificationRequestPacket* packet = &v as VerificationRequestPacket*;

            if (Logs)
            {
                print("I-REQ ");
                print(packet.position);
                printline();
            }

            if (distancesqr(packet.position, GPS.position) < DistanceThresholdSq)
            {
                if (Logs) print(" M");
                VerificationResponsePacket responsePacket = new VerificationResponsePacket;
                responsePacket.header = PacketHeader.response;
                responsePacket.position = GPS.position;
                if (Debug) debug(packet.position, Color.Cyan);
                send(&responsePacket as byte[]*, sizeof(VerificationResponsePacket));
            }
        }
    }
}

void PerformRadar()
{
    //ldebug(new float3(cos(Radar.direction), 0f, sin(Radar.direction)) * 10f, Color.Cyan);
    float3 radarPoint = radar();
    if (!empty(radarPoint))
    {
        toglobal(&radarPoint);

        RadarPointPacket radarPointPacket;
        radarPointPacket.header = PacketHeader.radar_point;
        radarPointPacket.position = radarPoint;
        radarPointPacket.speedSignal = Radar.response.speedSignal;
        radarPointPacket.clutter = Radar.response.clutter;
        radarPointPacket.fingerprint = Radar.response.fingerprint;
        radarPointPacket.meta = Radar.response.meta;
        send(&radarPointPacket as byte[]*, sizeof(RadarPointPacket));
    }
    Radar.direction = (Radar.direction + 0.01f) % TAU;
}

printline("Started ...");

while (1)
{
    Receive();

    PerformRadar();
}
