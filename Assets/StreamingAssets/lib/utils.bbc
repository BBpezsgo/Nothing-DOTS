export inline bool empty(float3 v)
{
    return !((v.x as int) || (v.y as int) || (v.z as int));
}

export void fill<T>(T[]* buffer, int length, T value)
{
    for (int i = 0; i < length; i++)
    {
        buffer[i] = value;
    }
}

export void fill<T>(T[]* buffer, int start, int length, T value)
{
    length += start;
    for (int i = start; i < length; i++)
    {
        buffer[i] = value;
    }
}
