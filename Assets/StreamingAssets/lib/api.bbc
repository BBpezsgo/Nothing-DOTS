using "stdlib/System.bbc";
using "mm.bbc";
using "utils.bbc";

export void print(float2 v)
{
    print(v.x);
    print(' ');
    print(v.y);
}

export inline void printline(float2 v)
{
    print(v);
    printline();
}

export void print(float3 v)
{
    print(v.x);
    print(' ');
    print(v.y);
    print(' ');
    print(v.z);
}

export inline void printline(float3 v)
{
    print(v);
    printline();
}

export void sleep(float sec)
{
    float at = time() + sec;
    while (time() < at) { }
}

[External("atan2")] export float atan2(float y, float x);
[External("tan")] export float tan(float v);
[External("asin")] export float asin(float v);
[External("acos")] export float acos(float v);
[External("atan")] export float atan(float v);

[External("wreceive")] export int wreceive(any* buffer, int length, float3& direction);
[External("wsend")] export void wsend(any* buffer, int length, float3& direction, float angle);

[External("receive")] export int receive(any* buffer, int length, byte port = 0);
[External("send")] export void send(any* buffer, int length, byte port = 0);

export inline void wsend(any* buffer, int length, float3 direction, float angle)
{
    wsend(buffer, length, &direction, angle);
}

export inline void wsend(any* buffer, int length, float3& direction)
{
    wsend(buffer, length, direction, 0.1f);
}

export inline void wsend(any* buffer, int length, float3 direction)
{
    wsend(buffer, length, &direction, 0.1f);
}

export inline void wsend(any* buffer, int length)
{
    wsend(buffer, length, (float3*)0, 0f);
}

[External("radar")] void _radar();

export float3 radar(float direction)
{
    Radar.direction = direction;
    //ldebug(new float3(cos(Radar.direction), 0f, sin(Radar.direction)) * 10f, Color.Cyan);
    return radar();
}

export float3 radar()
{
    Radar.response.point.x = 0f;
    Radar.response.point.y = 0f;
    Radar.response.point.z = 0f;

    _radar();

    while (Radar.response.point.x == 0f || Radar.response.point.y == 0f || Radar.response.point.z == 0f)
    {

    }

    if (isnan(Radar.response.point.x) || isnan(Radar.response.point.y) || isnan(Radar.response.point.z))
    {
        return new float3;
    }

    return Radar.response.point;
}

export void toglobal(byte3* point)
{
    float3 p;
    tofloat3(point, &p);
    toglobal(&p);
    tobyte3(&p, point);
}

export void toglobal(byte2* point)
{
    float2 p;
    tofloat2(point, &p);
    toglobal(&p);
    tobyte2(&p, point);
}

[External("toglobal")] export void toglobal(float3* point);
[External("tolocal")] export void tolocal(float3* point);

[External("toglobald")] export void toglobald(float3* direction);
[External("tolocald")] export void tolocald(float3* direction);

export float3 tolocal(float3 point)
{
    tolocal(&point);
    return point;
}

export float3 toglobal(float3 point)
{
    toglobal(&point);
    return point;
}

export float3 tolocald(float3 direction)
{
    tolocald(&direction);
    return direction;
}

export float3 toglobald(float3 direction)
{
    toglobald(&direction);
    return direction;
}

export void toglobal(float2* point)
{
    float3 _p = new float3(point.x, 0f, point.y);
    toglobal(&_p);
    *point = new float2(_p.x, _p.z);
}

export void tolocal(float2* point)
{
    float3 _p = new float3(point.x, 0f, point.y);
    tolocal(&_p);
    *point = new float2(_p.x, _p.z);
}

[External("time")] export float time();

export enum Color : byte
{
    Blue = 0b001,
    Green = 0b010,
    Cyan = 0b011,
    Red = 0b100,
    Magenta = 0b101,
    Yellow = 0b110,
    White = 0b111,
}

[External("debug")] export void debug(float3 position, Color color);
[External("ldebug")] export void ldebug(float3 localPosition, Color color);

export inline void debug(float2 position, Color color)
{
    debug(new float3(position.x, 0f, position.y), color);
}

export inline void ldebug(float2 localPosition, Color color)
{
    ldebug(new float3(localPosition.x, 0f, localPosition.y), color);
}

[External("debug_label")] export void debug_label(float3 position, temp string text);
[External("ldebug_label")] export void ldebug_label(float3 localPosition, temp string text);

export inline void debug_label(float2 position, temp string text)
{
    debug_label(new float3(position.x, 0f, position.y), temp text);
}

export inline void ldebug_label(float2 localPosition, temp string text)
{
    ldebug_label(new float3(localPosition.x, 0f, localPosition.y), temp text);
}

[External("dequeue_command")] export int dequeue_command(any* data);

[External("attributes_query")] export bool try_query_attribute(int attribute, any* data);

export void query_attribute(int attribute, any* data)
{
    if (!try_query_attribute(attribute, data)) crash "Failed to query attribute";
}

export const int ATTRIBUTE_TURRET_MAGAZINE = 0;
export const int ATTRIBUTE_TURRET_PROJECTILE_VELOCITY = 1;
export const int ATTRIBUTE_TURRET_PROJECTILE_DAMAGE = 2;

[External("gui_create")] void gui_create(any* ptr);
[External("gui_update")] void gui_update(any* ptr);
[External("gui_destroy")] void gui_destroy(int id);

export byte colorto255(float r, float g, float b)
{
    return (byte)(
        ((byte)(r * 7) << 5) |
        ((byte)(g * 7) << 2) |
        ((byte)(b * 3))
    );
}

export byte colorto255(int r, int g, int b)
{
    return (byte)(
        ((byte)(r * 7 / 255) << 5) |
        ((byte)(g * 7 / 255) << 2) |
        ((byte)(b * 3 / 255))
    );
}

export struct UILabel
{
    private bool _dirty;
    private int _id;
    private byte _type;
    int x;
    int y;
    int width;
    int height;
    float3 color;
    char[15] text;

    export UILabel()
    {
        this._type = 1;
        this.x = 50;
        this.y = 50;
        this.width = 50;
        this.height = 50;
        this.text = "Hey";
        gui_create(&this);
    }

    export inline void destroy()
    {
        gui_destroy(this._id);
    }

    export inline void update()
    {
        gui_update(&this);
    }
}

export struct UIImage
{
    private bool _dirty;
    private int _id;
    private byte _type;
    int x;
    int y;
    int width;
    int height;
    i16 imgwidth;
    i16 imgheight;
    byte[510] img;

    export UIImage(i16 width, i16 height)
    {
        this._type = 2;
        this.x = 50;
        this.y = 50;
        this.width = 50;
        this.height = 50;
        this.imgwidth = width;
        this.imgheight = height;
        // for (int y = 0; y < height; y++)
        // {
        //     for (int x = 0; x < width; x++)
        //     {
        //         this.img[x + (y * width)] = (byte)(x + (y * width));
        //     }
        // }
        gui_create(this);
    }

    export inline void destroy()
    {
        gui_destroy(this._id);
    }

    export inline void set(int x, int y, int r, int g, int b)
    {
        this.set(x, y, colorto255(r, g, b));
    }

    export inline void fill(int r, int g, int b)
    {
        this.fill(colorto255(r, g, b));
    }

    export void set(int x, int y, byte color)
    {
        if (x < 0) x = 0;
        else if (x >= this.imgwidth) x = (int)this.imgwidth;
        if (y < 0) y = 0;
        else if (y >= this.imgheight) y = (int)this.imgheight;
        this.img[y * this.imgwidth + x] = color;
    }

    export void fill(byte color)
    {
        int l = (int)this.imgwidth * (int)this.imgheight;
        for (int i = 0; i < l; i++)
        {
            this.img[i] = color;
        }
    }

    export inline void update()
    {
        gui_update(this);
    }

    // export inline void set(int2 pos, int r, int g, int b)   { this.set(pos.x, pos.y, r, g, b); }
    // export inline void set(int2 pos, byte color)              { this.set(pos.x, pos.y, color); }

    export inline void set(float2 pos, int r, int g, int b) { this.set((int)pos.x, (int)pos.y, r, g, b); }
    export inline void set(float2 pos, byte color)            { this.set((int)pos.x, (int)pos.y, color); }
}

[External("pendrive_plug")]
export void _pendrive_plug();

[External("pendrive_unplug")]
export void _pendrive_unplug();

[External("pendrive_read")]
export int _pendrive_read(int source, byte[]* destination, int length);

[External("pendrive_write")]
export int _pendrive_write(byte[]* source, int destination, int length);

export void _facility_enqueue(TechnologyHash* hash)
{
    Facility.hash_location = hash;
    Facility.signal = (byte)1;
    while (Facility.signal == (byte)1)
    {

    }
}

export byte _facility_dequeue(TechnologyHash* hash)
{
    Facility.hash_location = hash;
    Facility.signal = (byte)2;
    while (Facility.signal == (byte)2)
    {

    }
    return Facility.signal == (byte)3;
}
